/**
 * Barrel file
 */

export { config } from "./config.js";

// If you want to import `lazyConfig`, import it directly instead of loading it
// through this barrel file, because if you load it through this file, `config`
// module will also be ran and loaded!
